# 安全边界

- 当前版本仅面向可信单用户本机运行，Node服务监听127.0.0.1。尚未进行独立安全审计，不要部署为公网共享服务。
- 渲染HTML会清理危险内容；这不代表所有浏览器或第三方渲染库都已无漏洞。
- WebDAV密码默认仅在服务内存中；显式保存使用当前Windows用户DPAPI加密文件（用户目录.writide/webdav.dpapi），不写项目或浏览器明文存储。该账户下的恶意程序仍可能读取，勿在共用不可信账户保存。会话令牌属于敏感数据，不要贴出凭据、服务响应正文、私人笔记或未脱敏trace。
- WebDAV覆盖使用条件请求，冲突必须人工处理。不要通过移除If-Match绕过冲突检查。
- `.gitignore`不清理Git历史。发布前必须审查既有提交中的私人文件、日志和地址。
- 图片磁盘缓存默认在用户目录.writide/image-cache，包含未加密图片字节及路径/版本元数据，不含应用密码。按私人资料管理目录权限，不纳入公共仓库或镜像；WRITIDE_CACHE_DIR应指定专用目录，单个服务进程写入，旧INKQUAY_CACHE_DIR仍作为兼容别名。缓存不是备份，清理只处理受管缓存文件，不删除源文档或远端图片。
- 本项目尚未建立公开安全联系渠道。发布者应在开放仓库前设置GitHub私密漏洞报告或安全邮箱；不要在公开Issue贴出可利用的凭据或私人数据。
