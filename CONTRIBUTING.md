# 贡献说明

1. 阅读README、SECURITY.md和相关模块，再建立最小复现。
2. 使用一个Markdown/CodeMirror模型，不引回多个编辑块或从DOM反向保存Markdown。
3. 修复先提供真实键鼠反例，再验证源范围、视觉几何及保存/Undo。优先定向运行，阶段性完整回归。
4. 公开贡献使用 `playwright.portable.config.ts`。不要提交私人教程、网盘账号、图片、工作区内容或测试输出。
5. 一个PR处理一个明确目标，说明修改、测试方法和未覆盖边界。不要因旧DOM测试失败而恢复废弃架构。
6. 协议/工作区改动必须包含失败路径：断网、拒绝权限、冲突、重连、文件外部删除与不覆盖源数据。
7. 提交贡献即表示你有权按本项目MIT许可证提供相应代码；不要复制来源或许可不明确的实现。
